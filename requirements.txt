# Core numerics — scoring model, simulation, analysis scripts
numpy
scipy~=1.16.0
pandas~=2.3.1
numba          # JIT for src/simulation/sim.py

# Plotting / reporting
matplotlib~=3.10.3
plotly

# Board detection from photos
Pillow         # PIL, used by processing_detected_images
requests       # talks to the local TensorFlow Serving container
google-cloud-aiplatform>=1.0.0

# Static site (content/ -> output/)
pelican
pelican-jinja2content
pelican-render-math
markdown
Fabric

# YOLO training has heavier, separate deps:
#   pip install -r src/training_data_generator/requirements_yolo.txt
